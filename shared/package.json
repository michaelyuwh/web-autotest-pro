{
  "name": "@web-autotest-pro/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Web AutoTest Pro",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "typescript": "^5.2.0"
  }
}
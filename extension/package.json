{
  "name": "@web-autotest-pro/extension",
  "version": "1.0.0",
  "description": "Web AutoTest Pro Browser Extension",
  "scripts": {
    "build": "webpack --mode=production",
    "build:firefox": "webpack --mode=production --env=firefox",
    "dev": "webpack --mode=development --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@web-autotest-pro/shared": "*",
    "webextension-polyfill": "^0.10.0"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.10.0",
    "copy-webpack-plugin": "^11.0.0",
    "ts-loader": "^9.5.0",
    "typescript": "^5.2.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}